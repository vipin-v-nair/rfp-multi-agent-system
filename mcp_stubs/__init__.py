"""
Legacy MCP stubs — kept for reference only.

These direct Python function stubs have been replaced by real MCP servers
in the mcp_servers/ directory. Agents no longer import from this package;
they connect to the MCP servers via MCPToolset (stdio transport) instead.
"""
